[project]
name = "rss-aggregator-collector"
version = "0.1.0"
description = "RSS collection job (Cloud Run Job) for rss-aggregator"
requires-python = ">=3.12"
dependencies = [
    "rss-aggregator-shared",
    "feedparser>=6.0.11",
]

[project.optional-dependencies]
cloud = [
    "rss-aggregator-shared[cloud]",
]

[dependency-groups]
dev = [
    "pytest>=8.3.0",
    "ruff>=0.16.4",
]

[build-system]
requires = ["setuptools>=75.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["collect*"]

[tool.uv.sources]
rss-aggregator-shared = { workspace = true }

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
